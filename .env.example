# Required for the standalone TypeScript orchestrator (npm run start).
# In Claude Code (CLI / VS Code extension) this is not needed — your Claude
# subscription handles auth.
ANTHROPIC_API_KEY=sk-ant-xxxxx
